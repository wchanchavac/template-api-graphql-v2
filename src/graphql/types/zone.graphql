type Zone {
  id: UUID
  name: String
  region: Region
}

type PageZone {
  rows: [Zone]
  length: Int
  pages: Int
}

input CreateZone {
  name: String!
  regionId: UUID!
}

input UpdateZone {
  id: UUID!
  name: String
  regionId: UUID!
}

type Query {
  zones(options: Options = { page: 1 }): PageZone
  # zones(options: Options = { page: 1 }): [Zone]
  zone(id: UUID!): Zone
}

type Mutation {
  createZone(input: CreateZone!): Zone
  updateZone(input: UpdateZone!): Zone
  deleteZone(id: UUID!): Zone
}
