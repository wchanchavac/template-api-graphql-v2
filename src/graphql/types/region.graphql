type Region {
  id: UUID
  name: String
  abbreviation: String
  organization: Organization
}

type PageRegion {
  rows: [Region]
  length: Int
  pages: Int
}

input CreateRegion {
  name: String!
  abbreviation: String!
  organizationId: UUID!
}

input UpdateRegion {
  id: UUID!
  name: String
  abbreviation: String
  organizationId: UUID!
}

extend type Query {
  regions(options: Options = { page: 1 }): PageRegion
  # regions(options: Options = { page: 1 }): [Region]
  region(id: UUID!): Region
}

extend type Mutation {
  createRegion(input: CreateRegion!): Region
  updateRegion(input: UpdateRegion!): Region
  deleteRegion(id: UUID!): Region
}
